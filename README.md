# Regex Crystal Ball 🔮

Stop debugging regex patterns with trial and error. This tool tests your regex against multiple strings and shows exactly what matches (and what doesn't). Run: `python regex_crystal_ball.py 'your_pattern' 'test1' 'test2' ...`